import styled from "styled-components";

const BottomWrapper = styled.div`
  display: flex;
  margin-top: 8px;

  .time {
    margin-right: 20px;
  }

  .main {
    .icon {
      display: flex;
    }
  }
`;

export default BottomWrapper;
